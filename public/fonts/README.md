# Custom fonts for STORYSCORE

| File | Role |
|------|------|
| `PPFormula-CondensedBold.woff2` | Headlines (STORYSCORE QUIZ, CORE, BALANCE, INVERSE) |
| `PPNeueMontreal-Medium.woff2` | Body copy, buttons, descriptions |

Configured in `lib/fonts.ts` via `next/font/local`.

## Adding or changing fonts

1. Place `.woff2` files in this folder.
2. Update the `src` paths in `lib/fonts.ts`.
3. Restart the dev server.

## Licensing

Only host fonts you are licensed to use on the web.
